jwt:
  secret: ${JWT_SECRET}


spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/task_tracker_backend
    driver-class-name: org.postgresql.Driver